"""app/services/__init__.py — Service layer package.

Service classes encapsulate all business logic and interact with
models via SQLAlchemy.  They are framework-agnostic — no Flask
request/response objects should appear here.
"""
