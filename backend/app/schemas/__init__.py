"""app/schemas/__init__.py — Marshmallow schema package.

Schemas handle request validation and response serialization.
They are the boundary between HTTP layer (routes) and domain layer
(services + models).
"""
